package mx.raze.geomaps;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeAssetResourceIT extends AssetResourceTest {

    // Execute the same tests but in native mode.
}